Key = "55555" 

local function CheckKey(playerKey)
    if playerKey == Key then
        print("Key ถูกต้อง")
    else
        print("Key ไม่ถูกต้อง")
        local kickMessage = "คีย์ไม่ถูกโปรดตรวจสอบคีย์ให้ดี"
    end
end

CheckKey(userInputKey)
